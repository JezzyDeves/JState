{
  "name": "jstate",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./index.ts --outdir ./dist --minify",
    "build:watch": "bun build ./index.ts --outdir ./dist --minify --watch",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@preact/signals-core": "^1.6.0",
    "immer": "^10.1.1",
    "zod": "^3.23.8"
  }
}
